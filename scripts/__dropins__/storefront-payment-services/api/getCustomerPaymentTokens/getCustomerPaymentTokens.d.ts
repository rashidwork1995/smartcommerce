import { NormalizedVaultToken } from '../../data/models/vault-token';

/**
 * Loads customer payment tokens via this package’s GraphQL client (`initialize` sets endpoint / Store).
 */
export declare function getCustomerPaymentTokens(): Promise<NormalizedVaultToken[]>;
//# sourceMappingURL=getCustomerPaymentTokens.d.ts.map